stages:
  - test

playwright_tests:
  stage: test

  before_script:
    - npm ci
    - npx playwright install

  script:
    - npx playwright test

  artifacts:
    when: always
    paths:
      - playwright-report/
      - test-results/
    reports:
      junit: test-results/junit.xml
    expire_in: 7 days